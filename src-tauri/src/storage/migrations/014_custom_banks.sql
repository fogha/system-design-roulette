-- A learner's own class may carry a question bank the tutor wrote: three
-- cited questions per stage, the shape the placement check and unit
-- challenges sample. Absent until written.
ALTER TABLE custom_courses ADD COLUMN bank_json TEXT;
